
export PATH=$PATH:~/bin

# simple aliases
alias ls='ls --color=auto'
alias ll='ls -la'
alias grep='grep --color'
alias c='clear'

alias cm=chezmoi

EDITOR=vim

if [ -f ~/.bashrc ]; then
	source ~/.bashrc
fi
